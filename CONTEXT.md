# CONTEXT.md

> 本文件记录本项目的领域语言与术语。它是 `improve-codebase-architecture` / `diagnose` / `tdd` / `grill-with-docs` 等 skills 读取的"词汇表"——当输出里需要命名一个领域概念时，从这里取词，避免各自生造近义词。
>
> **不要在这里编造术语。** 只写已经真实存在于代码或对话里的词。如果项目结构暗示存在某个概念（例如脚手架里预留了 `src/modules/user/`），先不要假设它的业务含义——等业务含义在对话里浮现、并经 `/grill-with-docs` 确认后再补进来。

## 项目概览

- **项目代号**: base-project-java 仓库
- **仓库类型**: monorepo
- **交流 / 输出语言**: 中文
- **脚手架占位内容**: 脚手架里已出现大量"占位"示例（如 HelloWorld、UserTable、示例组件等），这些**不是真正的业务概念**，只是脚手架产物。真正的业务领域术语应来自后续的业务对话，而不是反向推导脚手架示例。

## 目录结构

```
/
├── react-admin/      # 前端（管理后台 SPA）
├── base-api/         # 后端（Java API）
├── record/           # 项目过程记录
├── docs/
│   ├── adr/          # 架构决策记录（ADR）
│   └── agents/       # Agent skills 的仓库级配置
└── task/             # 任务上下文（读取范围边界时参考）
```

## 技术栈（已确认，来自 manifest）

### 前端 react-admin
- 框架: React 19 + TypeScript + Vite 6
- 路由: react-router-dom v7
- 状态管理: Zustand
- 表单: react-hook-form + zod（@hookform/resolvers）
- 样式: Tailwind CSS + class-variance-authority + tailwind-merge
- 图标: lucide-react
- Lint: ESLint + typescript-eslint

### 后端 base-api
- 框架: Spring Boot 3（Java）
- ORM: MyBatis-Plus（含 jsqlparser）
- 认证/鉴权: Sa-Token
- 验证: spring-boot-starter-validation
- 横切关注点: spring-boot-starter-aop
- 运行时依赖（来自 pom）: MySQL、Redis（sa-token-redis 集成）

## 已知领域术语

| 术语 | 同义词 / 禁用词 | 定义 |
| --- | --- | --- |
| 车辆 | 车辆实体、车 | 归属于某个煤矿、可被系统监控的业务对象；不要求必须通过 GCAN 盒子接入。 |
| 车辆档案 | 车辆信息、车辆基础信息 | 描述一辆车辆的基础业务资料，用于识别车辆并关联其监控数据；车辆必须归属一个煤矿档案项。 |
| 停用车辆 | 禁用车辆 | 已被排除出车辆监控展示和实时状态处理的车辆；不会因外部接口再次发现而自动恢复启用。 |
| 车辆类型 | 车型、车型编码 | 用于车辆分类；对 GCAN 接入车辆还用于选择 CAN 数据解析规则，车型编码匹配时按大写形式识别。 |
| 外部接口车辆类型 | `EXTERNAL` 车型 | 外部车辆自动建档时使用的默认车型编码；表示该车辆不依赖 GCAN 协议解析，后续可由人工改为实际车型。 |
| 煤矿档案项 | 煤矿、煤矿字典项 | 车辆所属煤矿的业务标识与名称。 |
| 外部煤矿配置 | 煤矿接口配置、接入煤矿配置 | 由 `mine-config/list` 提供的外部接入煤矿身份、接口地址和数据更新约束；接口是该配置的权威来源，暂时不可用不删除本地档案。 |
| 外部数据源配置 | 外部接口配置、车辆接口配置 | 系统维护的外部车辆数据源连接配置，包括服务地址、接口路径、请求约束和是否启用；支持线上修改，并可由环境配置提供线下默认值。 |
| 外部数据源启用状态 | 接口开关、外部接口开关 | 控制系统是否请求外部车辆数据；关闭时不访问外网，不影响本地车辆档案和 GCAN 接入。 |
| 数据源异常 | 接口异常、接入异常 | 某个车辆数据源在本次同步中无法成功读取或处理的状态；不等同于车辆暂无数据、离线或未支持解析。 |
| GCAN 盒子 | 盒子、CAN 盒子、设备 | 一种安装或绑定在车辆上的数据采集设备，负责上报车辆 CAN 数据；不是所有车辆都必须具备。 |
| 盒子绑定 | 车辆盒子绑定、设备绑定 | 车辆与 GCAN 盒子之间的对应关系，仅适用于 GCAN 接入方式。 |
| 外部车辆编码 | 外部车辆 ID、车辆编码 | 外部煤矿系统分配给车辆的标识，例如 `R101`；与 GCAN 盒子标识属于不同接入体系。 |
| 接入方式 | 数据源类型、车辆数据源 | 车辆监控数据进入系统的方式，至少包括 GCAN 接入和煤矿接口接入。 |
| 主接入方式 | 主数据源 | 一辆车辆当前用于生成车辆监控状态的唯一接入方式；同车出现多路数据时必须先解决数据源冲突。 |
| 外部接入车辆档案 | 自动车辆档案、接口车辆档案 | 由煤矿接口发现并进入系统的车辆档案；以煤矿和外部车辆编码识别，接口暂时不返回时保留档案而不自动删除。 |
| 外部车辆身份 | 接口车辆身份 | 由煤矿编码和外部车辆编码共同构成的稳定车辆标识；车辆展示名称和业务配置变化不影响该身份，外部编码变化视为新车辆身份。 |
| 车辆展示名称 | 车辆名称、显示名称 | 系统中供值守人员识别车辆的名称，可由人工修改，不作为外部车辆身份。 |
| 车辆 CAN 状态 | 当前状态、车辆状态 | 由车辆 CAN 数据解析出的车辆运行状态。 |
| 车辆监控状态 | 统一车辆状态、实时车辆状态 | 由任一接入方式产生的车辆当前运行状态；车辆 CAN 状态是其中的 GCAN 接入子集。 |
| 协议解析器 | 车型协议处理器、CAN 解析器 | 根据车辆类型和 CAN ID 将原始 CAN 帧记录解释为车辆 CAN 状态的规则集合。 |
| 实时快照 | 当前快照、最新状态 | 系统当前看到的某辆车辆的最新统一监控状态，更新时间来自对应的数据源。 |
| CAN 历史数据 | 历史数据、CAN 历史 | 已接收并保留下来的 CAN 数据记录，用于后续追溯和查询；煤矿等业务筛选归属来自车辆档案。 |
| 原始 CAN 帧记录 | 原始帧、CAN 帧记录 | GCAN 盒子上报的一条未解析为车辆状态的 CAN 数据记录。 |
| 开发环境原始 CAN 帧镜像 | 线上 CAN 数据同步、原始 CAN 同步 | 仅供开发环境使用：按选定的 GCAN 盒子及可选 CAN ID 范围，以周期采样方式从线上开放接口获得当前原始 CAN 帧快照，并在开发环境重新作为原始 CAN 帧输入的数据副本，用于继续验证开发环境的车辆状态解析和生命周期行为；不承诺逐帧复制线上产生的全部数据，且不能与开发环境的本地 GCAN TCP 输入并存。镜像不导入线上车辆档案，开发环境必须独立维护相同盒子绑定和协议配置，并沿用线上原始帧的接收时间判断数据新鲜度。镜像帧进入车辆 CAN 状态解析和 CAN 历史数据处理链，历史写入仍由既有规则控制。临时无法读取线上接口时保留已有镜像并允许其自然过期；成功读取后则以线上当前结果覆盖选定范围。 |
| CAN ID | 报文 ID、帧 ID | 原始 CAN 帧记录中的标识，用于区分不同类型的 CAN 数据。 |
| 在线状态 | 在线/离线状态 | 系统根据车辆对应数据源的最新数据时间和该数据源的有效时限判断出的连接活跃状态。 |
| 车辆连接状态 | 在线、离线、暂无数据 | 描述启用车辆当前数据连接情况的状态维度；在线、离线和暂无数据分别表示数据在数据源有效时限内更新、曾有数据但已超过有效时限、尚未收到过数据。 |
| 车辆解析状态 | 已支持解析、未支持解析 | 描述系统是否能将车辆接入数据转换为统一车辆监控状态的状态维度；GCAN 接入依赖协议解析器，煤矿接口接入不依赖 CAN 协议解析器，与车辆连接状态相互独立。 |
| 车辆故障码表 | 故障码表、故障映射表 | 为一个具体车型或车型协议版本独立维护的车辆故障码定义资料；即使多个车型的名称、等级和处理方式相同，也分别保存。车辆类型选择 CAN 解析规则，故障码表选择故障码翻译规则。 |
| 故障码表编码 | 故障码版本、故障配置 | 车辆关联的稳定故障码表标识，用于和故障码共同确定故障名称、等级及处理信息；不直接使用手册工作表名称作为编码。 |
| 故障码 | 故障代码 | 由车辆接入数据中的 `faultState` 或 `faultCode` 表示的单个故障标识；`0` 表示无故障，非零值按车辆关联的故障码表翻译，未匹配时保留原始故障码并仍视为故障。 |
| 故障等级 | 严重程度、故障级别 | 故障码资料对故障影响程度的分类，用于卡片提示和故障详情展示；不同手册可能使用致命、严重、一般或分级编码。 |
| 监控大屏 | 大屏 | 按煤矿分组展示所有启用车辆的独立全屏监控页面，并兼容手机端响应式布局；车辆没有实时快照时仍保留卡片并明确标记暂无数据或离线，面向值守人员阅读，不等同于车辆档案或状态管理列表。 |
| 对外 API | 外部接口、开放接口 | 面向大屏前端和第三方系统提供车辆实时监控数据的只读接口；当前阶段第三方匿名访问，允许读取全部实时快照字段和对应接入标识，并单独提供当前原始 CAN 帧和分页查询的 CAN 历史数据；同时返回稳定编码与展示名称，其数据契约与后台管理接口分开考虑。 |

## 边界与职责约定（已确认）

- **前端改动范围**: `react-admin/src/**`，不要新建顶层 `src/`。
- **后端改动范围**: `base-api/src/**`。
- **脚手架结构参考**: 只看单个子模块，不要批量扫目录。
- **架构参考**: 只在 `docs/` 或 `experience/` 下找摘要。

## 何时本文件需要更新

以下信号之一出现时，应触发本文件（和/或 `docs/adr/`）的更新，通常由 `/grill-with-docs` 完成：

- 业务对话里**首次出现**一个项目通用名词（例如"训问""任务单"）。
- 两个模块对同一个概念使用不同名字——应在这里趋向统一。
- 某次架构决策被沉淀下来（例如"为什么选 Zustand 而非 Redux"）——应写进 `docs/adr/`。

---

_本文件最初由 `/setup-matt-pocock-skills` 与 `/grill-with-docs` 协作初始化。后续每一条术语都应该有可追溯的业务来源。_
